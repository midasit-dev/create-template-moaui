import React from 'react';

import {
	GuideBox,
} from '@midasit-dev/moaui';

import JsonEditor from './JsonEditor';

const Home = () => {
	// const [
	// 	layoutGuideBoxSchema,
	// 	setLayoutGuideBoxSchema,
	// ] = anyArray;

	// React.useEffect(() => setLayoutGuideBoxSchema(undefined), [setLayoutGuideBoxSchema]);

	// return (
	// 	<GuideBox width="100%" row height='inherit'>
	// 		<JsonEditor anyArray={[layoutGuideBoxSchema, setLayoutGuideBoxSchema]} />
	// 	</GuideBox>
	// )
}

export default Home;
